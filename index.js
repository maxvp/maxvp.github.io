// List.JS

var options = {
    valueNames: [
        "title",
        { name: "date", attr: "real-date" },
        "type",
        "description",
    ],
};

var archiveList = new List("archive", options);
